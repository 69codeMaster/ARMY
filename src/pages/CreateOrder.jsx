import React from "react";
import FormEntry from "../components/UI/Form/Entry";

const CreateOrder = () => {
  return (
    <div>
      {/* <FormEntry  name={"status"} onChange={} placeholder={} />
      <FormEntry  name={} onChange={} placeholder={} />
      <FormEntry  name={} onChange={} placeholder={} />
      <FormEntry name={} onChange={} placeholder={}  />
      <FormEntry  name={} onChange={} placeholder={} /> */}
    </div>
  );
};

export default CreateOrder;
